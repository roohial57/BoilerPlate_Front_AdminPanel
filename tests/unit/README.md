### Run
`npm run test`


### Run and wait
`npm run test:w`

### Serve UI in browser
`npm run test:ui`




### Vs code extentions
Install the `Vitest` extention


### setup
Add global setup logic in `setup.ts`.
